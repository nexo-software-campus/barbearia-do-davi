# Site institucional Barbearia do Davi — Home, serviços e agendamento

Projeto gerado pelo Nexus Campus (Vite + React + Tailwind CSS (saída estática em dist)). Build: `npm ci && npm run build`. Saída: `dist`.
